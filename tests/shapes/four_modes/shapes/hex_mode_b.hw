import * from @std/primitives/units

shape HexagonB(width: Measurement):
    geometry:
        for i in 0..6:
            let angle = i * 60
            let rad = angle * 3.14159265 / 180
            Point(x: width / 2 * cos(rad), y: width / 2 * sin(rad))
